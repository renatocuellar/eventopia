import React from 'react';
import ReactDOM from 'react-dom';

const element = <h1>Hello, Eventopia from React!</h1>

const container = document.getElementById('app');

ReactDOM.render(element, container);